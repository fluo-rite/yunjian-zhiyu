{
  "name": "yunjian-zhiyu",
  "private": true,
  "packageManager": "pnpm@10.8.1",
  "scripts": {
    "dev:mobile": "pnpm --dir apps/mobile android",
    "dev:server": "cd apps/server && uv run fastapi dev app/main.py --host 0.0.0.0 --port 8000",
    "dev:worker": "cd apps/server && uv run arq app.workers.arq_worker.WorkerSettings",
    "lint:mobile": "pnpm --dir apps/mobile lint",
    "lint:server": "uv run --project apps/server ruff check .",
    "test:server": "uv run --project apps/server pytest",
    "check": "pnpm lint:mobile && pnpm lint:server && pnpm test:server"
  }
}
